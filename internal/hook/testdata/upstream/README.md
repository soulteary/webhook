# Upstream compatibility fixtures

`hooks.json` and `hooks.yaml` are the canonical example configurations from
`adnanh/webhook` commit `2bbdeb9f90b5f98c7b7b8976881efd6da9f69571`.
They are kept verbatim and loaded in strict mode by the compatibility test.
